# Batch data model

A savings batch contains a unique batch ID, owner address, target XLM amount in
stroops, lifecycle state, and creation/deposit/approval/withdrawal evidence.

Spending rows in the UI explain how round-ups are calculated. The Soroban batch
is authoritative for mainnet custody and state.
