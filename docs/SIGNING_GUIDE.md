# Wallet signing guide

Before signing a TabungPin action, verify:

- Freighter is on Stellar Mainnet;
- the source public key is correct;
- the contract ID matches the deployment manifest;
- the function matches the current batch state;
- batch ID and XLM amount are expected;
- fee and timeout are acceptable.

Never paste a secret key into the app or repository.
